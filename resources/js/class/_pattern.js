(function(window, document, undefined) {
    'use strict';

    var PatternClass = function()
    {
        this.init()
    }

    PatternClass.prototype.init = function() {

    };

    window.PatternClass = PatternClass

}(window, document));

