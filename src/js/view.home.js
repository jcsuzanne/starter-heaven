(function(window, document, undefined) {
    'use strict';

    SITE.home = (function()
    {

        var init = function()
        {

        }

        var after = function(_view)
        {

        }

        return {
                init : init
            ,   alfter : after
        }
    })();

}(window, document));