(function(window, document, undefined) {
    'use strict';

    SITE.home = (function()
    {

        var init = function()
        {

        }

        return {
                init : init
        }
    })();

}(window, document));